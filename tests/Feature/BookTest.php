<?php

namespace Tests\Feature;

// use Illuminate\Foundation\Testing\RefreshDatabase;

use App\Models\Book;
use App\Models\User;
use Tests\TestCase;

class BookTest extends TestCase
{
    /**
     * A basic test example.
     */
    public function testPopularBase(): void
    {
        //$book = Book::factory()->count(50)->create();
        //$user = User::factory()->count(36)->create();

        //$response = $this->actingAs($users)->get('/books/', $book->id);
        //$response->assertStatus(200);
    }
}
